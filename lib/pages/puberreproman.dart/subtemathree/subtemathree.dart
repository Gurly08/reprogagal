import 'package:flutter/material.dart';

class Subtemathree extends StatelessWidget {
  const Subtemathree({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ini halaman subtema tiga',style: TextStyle(color: Colors.deepPurple),),
    );
  }
}