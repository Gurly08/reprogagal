import 'package:flutter/material.dart';

class Subtemafour extends StatelessWidget {
  const Subtemafour({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Ini halaman subtema empat',style: TextStyle(color: Colors.deepPurple),),
    );
  }
}